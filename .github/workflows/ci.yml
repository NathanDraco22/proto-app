name: CI
on: 
    pull_request: 
        branches:
            - main

jobs:
    flutter-analyze:
        name: Run analyze
        runs-on: ubuntu-latest
        steps:
            - uses: actions/checkout@v3
            - uses: subosito/flutter-action@v2
              with:
                flutter-version: '3.13.6'
                channel: 'stable'
            - run: flutter --version
            - run: flutter pub get
            - run: flutter analyze


