targets:
  $default:
    builders:
      ferry_generator|graphql_builder:
        enabled: true
        options:
          schema: proto_app|lib/schema-utf8.graphql
      ferry_generator|serializer_builder:
        enabled: true
        options:
          schema: proto_app|lib/schema-utf8.graphql